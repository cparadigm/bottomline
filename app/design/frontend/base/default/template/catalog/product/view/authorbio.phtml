<?php
		$product = Mage::registry('current_product');
		
		if($product->getBookAuthorBio())
		{
			echo ($product->getBookAuthorBio()); 
		}
		else
		{
			echo "No author biography available at this time";
		}
?>

